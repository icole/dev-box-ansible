---
- name: Install base packages
  package:
    name: "{{item}}"
    state: present
  become: true
  with_items:
    - git
    - gpg
    - python-pip
    - python3
    - mongodb
    - mongodb-server
    - zsh
